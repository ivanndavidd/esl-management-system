# Django Backend
SECRET_KEY=django-insecure-change-me-in-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL
DB_NAME=esl_db
DB_USER=postgres
DB_PASSWORD=postgres

# CORS — origins yang boleh akses backend (pisahkan dengan koma)
CORS_ALLOWED_ORIGINS=http://localhost:8081

# Frontend — URL backend yang bisa diakses dari browser (gunakan IP/domain server)
VITE_API_URL=http://localhost:8888/api
